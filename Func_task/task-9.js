{
    function Print1_Num (Num) {
        for(let i = 1; i <= Num ; i++){
            console.log(i);
        }
    };

    let Isnumber = prompt(`Enter Number To be Print :`,);
    Print1_Num(Isnumber);
}