"use strict"
{
    let unit = 100 ;
    let amount = 0 ;
    let total = 0;
    let percentage = 0;

    if(unit < 100)
    {
        total = Number.parseInt(unit * 0.6) ;
    }else{
        console.log("this is not mach ")
    }
}